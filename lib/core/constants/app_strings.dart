/// Centralized strings for the application to support localization.
class AppStrings {
  /// Private constructor to prevent instantiation.
  AppStrings._();

  /// Calendar screen title.
  static const String calendar = 'Calendar';
}
